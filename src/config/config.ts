import en from "assets/locales/en.json";
import tig from "assets/locales/tig.json";

const config = {
    localization: {
        defaultLanguage: 'en',
        languages: [
            en,
            tig
        ]
    },
    survey: {
        defaultTargetParticipants: 10
    }
}

export default config;